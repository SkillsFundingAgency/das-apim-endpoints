query FindClosedVacancies($refs: [Long]) {
    vacancies(
        where: {
            status:  { eq: CLOSED }
            closedDate: { neq: null }
            vacancyReference:  { in: $refs }
        }
    )
    {
        ...AllVacancyFields
    }
}