query GetProviderTransferableVacancyDetails($id: UUID) {
    vacancies(
        where: {
            id:
            {
                eq: $id
            }
        }
    )
    {
        id,
        vacancyReference,
        title,
        status,
        trainingProvider_Name,
        legalEntityName,
        ukprn
    }
}