query GetEmployerReviewedVacanciesCreatedBefore($pointInTime: DateTime) {
    vacancies(
        where: {
        status: { eq: REVIEW },
        createdDate: { lt: $pointInTime },
        deletedDate: { eq: null },
        closedDate: { eq: null }
    }
    )
    {
        id
        vacancyReference
        createdDate
    }
}